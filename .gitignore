/target
/.idea
/.vscode
**/*.db
**/*.db-shm
**/*.db-wal
**/.DS_Store

# Credentials and secrets
*.env
pluggy.env
service-account*.json
*-service-account.json
# minisign keys — the secret key must NEVER be committed (ADR-0017)
*.key
phai.pub

# Personal finance data
finance/pluggy-config.json
finance/data/
config.toml

# Local private workspace overlays
/.private/
/.local/

# Local agent state (worktrees, locks, settings) — keep shared playbook bits tracked
.claude/*
!.claude/commands/
!.claude/settings.json

# Internal planning documents (not for public repo)
entrega1-ajustes-pendentes.md
gaps.plan.md

# sentrux local/session artifacts
.sentrux/baseline*
.sentrux/*.baseline*
.sentrux/session*
.sentrux/cache*
.sentrux/history*
.sentrux/*.json
.sentrux/*.db
.sentrux/*.sqlite

# pi-lens tool cache
.pi-lens/
