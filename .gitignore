/target
/.idea
/.vscode
**/*.db
**/*.db-shm
**/*.db-wal
**/.DS_Store

# Credentials and secrets
*.env
pluggy.env
service-account*.json
*-service-account.json

# Personal finance data
finance/pluggy-config.json
finance/data/
config.toml

# Local private workspace overlays
/.private/
/.local/

# Local agent state (worktrees, locks, settings)
.claude/

# Internal planning documents (not for public repo)
entrega1-ajustes-pendentes.md
gaps.plan.md
