/** Forecasts + templates + manual add — ported from the current dashboard
 *  tabs in Phase 2d. */
export const Forecasts = () => (
  <div>
    <h2 style={{ fontFamily: 'var(--font-display)' }}>Previsões</h2>
    <p className="mono" style={{ color: 'var(--muted)' }}>
      Forecasts e modelos — em construção.
    </p>
  </div>
)
