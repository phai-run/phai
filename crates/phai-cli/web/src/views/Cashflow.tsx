/** Cash evolution chart — wired to /api/chart in a follow-up (Phase 2d). */
export const Cashflow = () => (
  <div>
    <h2 style={{ fontFamily: 'var(--font-display)' }}>Caixa</h2>
    <p className="mono" style={{ color: 'var(--muted)' }}>
      Evolução de caixa — em construção.
    </p>
  </div>
)
